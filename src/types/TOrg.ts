export type TOrg = {
  name: string;
  description: string;
  adminEmail: string;
  adminName: string;
  id: string;
  createdAt: string;
  updatedAt: string;
  createdBy: string;
  role: string;
};

export type TOrgs = TOrg[];
